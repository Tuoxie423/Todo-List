page {
  min-height: 100vh;
  color: #1f2520;
  background: #f7f1e4;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

view,
text,
input,
button {
  box-sizing: border-box;
}

button {
  padding: 0;
  border-radius: 8px;
}

button::after {
  border: 0;
}
